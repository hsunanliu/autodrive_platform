[pytest]
; G2 整合測試預設只跑 tests/integration（外部依賴全用 fake，不需 DB/鏈/Walrus）。
; tests/legacy 是 hackathon 遺留（需 docker DB 與舊版 app 結構），不納入收集。
testpaths = tests/integration
norecursedirs = tests/legacy
asyncio_mode = auto
